'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var plugin = require('tailwindcss/plugin');

function _interopDefault (e) { return e && e.__esModule ? e : { default: e }; }

var plugin__default = /*#__PURE__*/_interopDefault(plugin);

// src/index.ts

// src/length.ts
var lengthUnits = [
  "cm",
  "mm",
  "Q",
  "in",
  "pc",
  "pt",
  "px",
  // Absolute units
  "em",
  "ex",
  "ch",
  "rem",
  "lh",
  "rlh",
  // Relative units
  "vw",
  "vh",
  "vmin",
  "vmax",
  // Viewport units
  "cqw",
  "cqh",
  "cqi",
  "cqb"
  // Container query units
];
var lengthRegExp = new RegExp(
  `^\\s*([+-]?[0-9]*\\.?[0-9]+(?:[eE][+-]?[0-9]+)?)(${lengthUnits.join("|")})?\\s*$`
);
var Length = class _Length {
  constructor(number, unit) {
    this.number = number;
    this.unit = unit;
  }
  /**
   * Returns the CSS text representation of this length
   */
  get cssText() {
    return `${this.number}${this.unit ?? ""}`;
  }
  /**
   * Creates a copy of this Length with the same values
   */
  clone() {
    return new _Length(this.number, this.unit);
  }
  /**
   * Returns a new Length with the number negated
   */
  negate() {
    return new _Length(-this.number, this.unit);
  }
  /**
   * Checks if this length is equal to another length
   */
  equals(other) {
    return this.number === other.number && this.unit === other.unit;
  }
  /**
   * Parses a raw value into a Length object
   * Returns null if the value cannot be parsed as a valid length
   *
   * @param raw - The value to parse (string, number, null, or undefined)
   * @returns A Length instance or null if parsing fails
   */
  static parse(raw) {
    if (raw === 0) return new this(0);
    if (typeof raw !== "string") return null;
    const trimmed = raw.trim();
    if (trimmed === "0" || parseFloat(trimmed) === 0) {
      const match2 = trimmed.match(lengthRegExp);
      return new this(0, match2?.[2]);
    }
    const match = trimmed.match(lengthRegExp);
    if (!match) return null;
    const number = parseFloat(match[1]);
    if (isNaN(number)) return null;
    return new this(number, match[2]);
  }
  /**
   * Parses a value with fallback to Tailwind spacing scale
   * If no unit is found, treats the value as a Tailwind spacing multiplier
   *
   * @param raw - The value to parse
   * @returns A Length instance or null if parsing fails
   */
  static parseWithSpacingFallback(raw) {
    if (typeof raw !== "string") return null;
    const trimmed = raw.trim();
    const parsed = this.parse(trimmed);
    if (parsed && parsed.unit) return parsed;
    const num = parseFloat(trimmed);
    if (!isNaN(num)) {
      return new this(num * 0.25, "rem");
    }
    return null;
  }
  /**
   * Converts a Length to rem units
   *
   * @param rootFontSize - Root font size for conversion (default: 16)
   * @returns A new Length in rem units, or null if conversion not possible
   */
  toRem(rootFontSize = 16) {
    if (!this.unit) return null;
    switch (this.unit) {
      case "rem":
        return this.clone();
      case "px":
        return new _Length(this.number / rootFontSize, "rem");
      case "em":
        return new _Length(this.number, "rem");
      default:
        return null;
    }
  }
  /**
   * Converts a Length to pixel units
   *
   * @param rootFontSize - Root font size for conversion (default: 16)
   * @returns A new Length in px units, or null if conversion not possible
   */
  toPx(rootFontSize = 16) {
    if (!this.unit) return null;
    switch (this.unit) {
      case "px":
        return this.clone();
      case "rem":
      case "em":
        return new _Length(this.number * rootFontSize, "px");
      default:
        return null;
    }
  }
};

// src/errors.ts
var errorCodes = {
  // Value parsing errors
  "missing-min": () => "Missing minimum value",
  "missing-max": () => "Missing maximum value",
  "invalid-min": (value) => `Invalid minimum value: "${value}"`,
  "invalid-max": (value) => `Invalid maximum value: "${value}"`,
  // Unit errors
  "mismatched-units": (start, end) => `Start "${start}" and end "${end}" units don't match`,
  "unsupported-unit": (unit) => `Unsupported unit "${unit}" - use rem, px, or em`,
  // Value validation errors
  "no-change": (value) => `Start and end values are both "${value}"`,
  "invalid-viewport": (viewport) => `Invalid viewport value: "${viewport}"`,
  // Breakpoint errors
  "mismatched-bp-units": (start, end) => `Start breakpoint "${start}" and end breakpoint "${end}" units don't match`,
  "no-change-bp": (value) => `Start and end breakpoints are both "${value}"`,
  "mismatched-bp-val-units": () => `Breakpoint and value units don't match`,
  // Accessibility errors
  "fails-sc-144": (failingViewport) => `Fails WCAG SC 1.4.4 at viewport ${failingViewport}`,
  "font-too-small": (minPx) => `Font size ${minPx}px may be too small for accessibility`,
  // Theme errors
  "theme-value-not-found": (key) => `Could not find theme value "${key}"`,
  // General errors
  "parse-failed": (value) => `Failed to parse fluid value: "${value}"`
};
var FluidError = class extends Error {
  name = "FluidError";
  /**
   * The error code associated with this error
   */
  code;
  constructor(message, code) {
    super(message);
    this.code = code;
  }
  /**
   * Creates a FluidError from an error code with type-safe arguments
   *
   * @param code - The error code
   * @param args - Arguments required by the error message generator
   * @returns A new FluidError instance
   */
  static fromCode(code, ...args) {
    const fn = errorCodes[code];
    const message = fn(...args);
    return new this(message, code);
  }
};
function throwError(code, ...args) {
  throw FluidError.fromCode(code, ...args);
}
function ok(value) {
  return { success: true, value };
}
function err(code, ...args) {
  return { success: false, error: FluidError.fromCode(code, ...args) };
}

// src/clamp.ts
var formatters = {};
function toPrecision(num, precision) {
  if (!formatters[precision]) {
    formatters[precision] = new Intl.NumberFormat("en-US", {
      maximumFractionDigits: precision,
      useGrouping: false
    });
  }
  return formatters[precision].format(num);
}
function getPrecision(num) {
  if (Math.floor(num) === num) return 0;
  return num.toString().split(".")?.[1]?.length || 0;
}
function clampNumber(min, n, max) {
  return Math.min(Math.max(n, min), max);
}
function calculateClamp(minValue, maxValue, options) {
  const { minViewport, maxViewport, rootFontSize, useRem } = options;
  let start = Length.parse(minValue);
  let end = Length.parse(maxValue);
  if (!start) {
    start = Length.parseWithSpacingFallback(minValue);
  }
  if (!end) {
    end = Length.parseWithSpacingFallback(maxValue);
  }
  if (!start || !end) return "";
  const startRem = start.toRem(rootFontSize);
  const endRem = end.toRem(rootFontSize);
  if (!startRem || !endRem) return "";
  const minRem = startRem.number;
  const maxRem = endRem.number;
  if (start.number === 0 && !start.unit) {
    start = new Length(0, end.unit);
  } else if (end.number === 0 && !end.unit) {
    end = new Length(0, start.unit);
  }
  const minViewportRem = minViewport / rootFontSize;
  const maxViewportRem = maxViewport / rootFontSize;
  if (minRem === maxRem) {
    return useRem ? `${toPrecision(minRem, 4)}rem` : `${toPrecision(minRem * rootFontSize, 4)}px`;
  }
  const precision = Math.max(
    getPrecision(minRem),
    getPrecision(maxRem),
    getPrecision(minViewportRem),
    getPrecision(maxViewportRem),
    2
  );
  const slope = (maxRem - minRem) / (maxViewportRem - minViewportRem);
  const intercept = minRem - slope * minViewportRem;
  if (Math.abs(slope) < 1e-4) {
    return useRem ? `${toPrecision(minRem, precision)}rem` : `${toPrecision(minRem * rootFontSize, precision)}px`;
  }
  const slopeVw = slope * 100;
  const clampMin = Math.min(minRem, maxRem);
  const clampMax = Math.max(minRem, maxRem);
  const minFormatted = useRem ? `${toPrecision(clampMin, precision)}rem` : `${toPrecision(clampMin * rootFontSize, precision)}px`;
  const maxFormatted = useRem ? `${toPrecision(clampMax, precision)}rem` : `${toPrecision(clampMax * rootFontSize, precision)}px`;
  const interceptFormatted = useRem ? `${toPrecision(Math.abs(intercept), precision)}rem` : `${toPrecision(Math.abs(intercept * rootFontSize), precision)}px`;
  const slopeFormatted = toPrecision(slopeVw, precision);
  let preferred;
  if (intercept === 0) {
    preferred = `${slopeFormatted}vw`;
  } else if (intercept > 0) {
    preferred = `${interceptFormatted} + ${slopeFormatted}vw`;
  } else {
    preferred = `${slopeFormatted}vw - ${interceptFormatted}`;
  }
  return `clamp(${minFormatted}, ${preferred}, ${maxFormatted})`;
}
function calculateNegativeClamp(minValue, maxValue, options) {
  const clampValue = calculateClamp(minValue, maxValue, options);
  if (!clampValue.startsWith("clamp(")) {
    return `-${clampValue}`;
  }
  return `calc(${clampValue} * -1)`;
}
function checkSC144(startNum, endNum, startBP, endBP, slope, intercept) {
  const zoom1 = (vw) => clampNumber(startNum, intercept + slope * vw, endNum);
  const zoom5 = (vw) => clampNumber(5 * startNum, 5 * intercept + slope * vw, 5 * endNum);
  if (5 * startNum < 2 * zoom1(5 * startBP)) {
    return { passes: false, failingViewport: startBP * 5, failingUnit: "rem" };
  }
  if (zoom5(endBP) < 2 * endNum) {
    return { passes: false, failingViewport: endBP, failingUnit: "rem" };
  }
  return { passes: true };
}
function checkAccessibility(minValue, options, utilityType) {
  if (!options.checkAccessibility || utilityType !== "text") {
    return { isValid: true };
  }
  const minLength = Length.parse(minValue);
  if (!minLength) return { isValid: true };
  const minPx = minLength.toPx(options.rootFontSize);
  if (!minPx) return { isValid: true };
  if (minPx.number < 12) {
    return {
      isValid: false,
      warning: `Fluid typography minimum size (${minPx.number}px) may be too small for accessibility. Consider using at least 12px for small text or 16px for body text.`
    };
  }
  return { isValid: true };
}
function parseFluidString(value) {
  if (typeof value !== "string") {
    return null;
  }
  const parts = value.split("/");
  if (parts.length !== 2) {
    return null;
  }
  const [min, max] = parts;
  if (!min || !max) {
    return null;
  }
  return { min: min.trim(), max: max.trim() };
}
function extractStringValue(value) {
  if (typeof value === "string") {
    return value;
  }
  if (Array.isArray(value) && value.length > 0) {
    const firstValue = value[0];
    if (typeof firstValue === "string") {
      return firstValue;
    }
  }
  if (value && typeof value === "object") {
    const obj = value;
    for (const key of ["fontSize", "value", "size"]) {
      if (typeof obj[key] === "string") {
        return obj[key];
      }
    }
  }
  return null;
}
function resolveThemeValue(value, themeValues) {
  if (typeof value !== "string") {
    return null;
  }
  if (Length.parse(value)?.unit) {
    return value;
  }
  const resolved = themeValues[value];
  if (resolved !== void 0) {
    const extracted = extractStringValue(resolved);
    if (extracted) {
      return extracted;
    }
  }
  if (/^-?\d+(\.\d+)?$/.test(value)) {
    const num = parseFloat(value);
    return `${num * 0.25}rem`;
  }
  return null;
}
function calculateClampAdvanced(minValue, maxValue, options, overrides) {
  const { rootFontSize, useRem, debug } = options;
  const minViewport = overrides?.minViewport ?? options.minViewport;
  const maxViewport = overrides?.maxViewport ?? options.maxViewport;
  const useContainerQuery = overrides?.useContainerQuery ?? options.useContainerQuery;
  const negate = overrides?.negate ?? false;
  let start = Length.parse(minValue) ?? Length.parseWithSpacingFallback(minValue);
  let end = Length.parse(maxValue) ?? Length.parseWithSpacingFallback(maxValue);
  if (!start) {
    return {
      result: "",
      validation: {
        valid: false,
        error: FluidError.fromCode("invalid-min", minValue)
      }
    };
  }
  if (!end) {
    return {
      result: "",
      validation: {
        valid: false,
        error: FluidError.fromCode("invalid-max", maxValue)
      }
    };
  }
  if (start.number === 0 && !start.unit) {
    start = new Length(0, end.unit);
  } else if (end.number === 0 && !end.unit) {
    end = new Length(0, start.unit);
  }
  if (options.validateUnits && start.number !== 0 && end.number !== 0) {
    if (start.unit !== end.unit) {
      return {
        result: "",
        validation: {
          valid: false,
          error: FluidError.fromCode(
            "mismatched-units",
            start.cssText,
            end.cssText
          )
        }
      };
    }
  }
  const startRem = start.toRem(rootFontSize);
  const endRem = end.toRem(rootFontSize);
  if (!startRem || !endRem) {
    return {
      result: "",
      validation: {
        valid: false,
        error: FluidError.fromCode(
          "unsupported-unit",
          start.unit || end.unit || "unknown"
        )
      }
    };
  }
  let minRem = startRem.number;
  let maxRem = endRem.number;
  if (negate) {
    minRem *= -1;
    maxRem *= -1;
  }
  const minViewportRem = minViewport / rootFontSize;
  const maxViewportRem = maxViewport / rootFontSize;
  if (minRem === maxRem) {
    const value = useRem ? `${toPrecision(minRem, 4)}rem` : `${toPrecision(minRem * rootFontSize, 4)}px`;
    return {
      result: value,
      validation: {
        valid: true,
        warning: `Start and end values are equal (${value})`
      }
    };
  }
  if (minViewportRem === maxViewportRem) {
    return {
      result: "",
      validation: {
        valid: false,
        error: FluidError.fromCode("no-change-bp", `${minViewport}px`)
      }
    };
  }
  const precision = Math.max(
    getPrecision(minRem),
    getPrecision(maxRem),
    getPrecision(minViewportRem),
    getPrecision(maxViewportRem),
    2
  );
  const slope = (maxRem - minRem) / (maxViewportRem - minViewportRem);
  const intercept = minRem - slope * minViewportRem;
  if (Math.abs(slope) < 1e-4) {
    const value = useRem ? `${toPrecision(minRem, precision)}rem` : `${toPrecision(minRem * rootFontSize, precision)}px`;
    return {
      result: value,
      validation: { valid: true }
    };
  }
  const slopeVw = slope * 100;
  const viewportUnit = useContainerQuery ? "cqw" : "vw";
  const clampMin = Math.min(minRem, maxRem);
  const clampMax = Math.max(minRem, maxRem);
  const minFormatted = useRem ? `${toPrecision(clampMin, precision)}rem` : `${toPrecision(clampMin * rootFontSize, precision)}px`;
  const maxFormatted = useRem ? `${toPrecision(clampMax, precision)}rem` : `${toPrecision(clampMax * rootFontSize, precision)}px`;
  const interceptFormatted = useRem ? `${toPrecision(Math.abs(intercept), precision)}rem` : `${toPrecision(Math.abs(intercept * rootFontSize), precision)}px`;
  const slopeFormatted = toPrecision(slopeVw, precision);
  let preferred;
  if (intercept === 0) {
    preferred = `${slopeFormatted}${viewportUnit}`;
  } else if (intercept > 0) {
    preferred = `${interceptFormatted} + ${slopeFormatted}${viewportUnit}`;
  } else {
    preferred = `${slopeFormatted}${viewportUnit} - ${interceptFormatted}`;
  }
  let result = `clamp(${minFormatted}, ${preferred}, ${maxFormatted})`;
  if (debug) {
    const debugComment = `/* fluid from ${start.cssText} at ${minViewport}px to ${end.cssText} at ${maxViewport}px${useContainerQuery ? " (container)" : ""} */`;
    result = `${result} ${debugComment}`;
  }
  return {
    result,
    validation: { valid: true }
  };
}
function validateFluidUnits(minValue, maxValue) {
  const start = Length.parse(minValue) ?? Length.parseWithSpacingFallback(minValue);
  const end = Length.parse(maxValue) ?? Length.parseWithSpacingFallback(maxValue);
  if (!start) {
    return {
      valid: false,
      error: FluidError.fromCode("invalid-min", minValue)
    };
  }
  if (!end) {
    return {
      valid: false,
      error: FluidError.fromCode("invalid-max", maxValue)
    };
  }
  if (start.number === 0 || end.number === 0) {
    return { valid: true };
  }
  if (start.unit !== end.unit) {
    return {
      valid: false,
      error: FluidError.fromCode(
        "mismatched-units",
        start.cssText,
        end.cssText
      )
    };
  }
  if (start.number === end.number) {
    return {
      valid: false,
      error: FluidError.fromCode("no-change", start.cssText)
    };
  }
  return { valid: true };
}
function createNegatedClamp(minValue, maxValue, options, overrides) {
  const { result } = calculateClampAdvanced(minValue, maxValue, options, {
    ...overrides,
    negate: true
  });
  return result;
}
function createContainerClamp(minValue, maxValue, options, overrides) {
  const { result } = calculateClampAdvanced(minValue, maxValue, options, {
    ...overrides,
    useContainerQuery: true
  });
  return result;
}

// src/utilities.ts
var fluidUtilities = {
  // Padding utilities
  "fl-p": { property: "padding", scale: "spacing" },
  "fl-px": { property: ["padding-left", "padding-right"], scale: "spacing" },
  "fl-py": { property: ["padding-top", "padding-bottom"], scale: "spacing" },
  "fl-pt": { property: "padding-top", scale: "spacing" },
  "fl-pr": { property: "padding-right", scale: "spacing" },
  "fl-pb": { property: "padding-bottom", scale: "spacing" },
  "fl-pl": { property: "padding-left", scale: "spacing" },
  "fl-ps": { property: "padding-inline-start", scale: "spacing" },
  "fl-pe": { property: "padding-inline-end", scale: "spacing" },
  // Margin utilities
  "fl-m": { property: "margin", scale: "spacing", supportsNegative: true },
  "fl-mx": {
    property: ["margin-left", "margin-right"],
    scale: "spacing",
    supportsNegative: true
  },
  "fl-my": {
    property: ["margin-top", "margin-bottom"],
    scale: "spacing",
    supportsNegative: true
  },
  "fl-mt": { property: "margin-top", scale: "spacing", supportsNegative: true },
  "fl-mr": {
    property: "margin-right",
    scale: "spacing",
    supportsNegative: true
  },
  "fl-mb": {
    property: "margin-bottom",
    scale: "spacing",
    supportsNegative: true
  },
  "fl-ml": {
    property: "margin-left",
    scale: "spacing",
    supportsNegative: true
  },
  "fl-ms": {
    property: "margin-inline-start",
    scale: "spacing",
    supportsNegative: true
  },
  "fl-me": {
    property: "margin-inline-end",
    scale: "spacing",
    supportsNegative: true
  },
  // Typography utilities
  "fl-text": { property: "font-size", scale: "fontSize" },
  "fl-leading": { property: "line-height", scale: "lineHeight" },
  "fl-tracking": { property: "letter-spacing", scale: "letterSpacing" },
  // Sizing utilities
  "fl-w": { property: "width", scale: "spacing" },
  "fl-h": { property: "height", scale: "spacing" },
  "fl-size": { property: ["width", "height"], scale: "spacing" },
  "fl-min-w": { property: "min-width", scale: "spacing" },
  "fl-max-w": { property: "max-width", scale: "spacing" },
  "fl-min-h": { property: "min-height", scale: "spacing" },
  "fl-max-h": { property: "max-height", scale: "spacing" },
  // Gap utilities
  "fl-gap": { property: "gap", scale: "spacing" },
  "fl-gap-x": { property: "column-gap", scale: "spacing" },
  "fl-gap-y": { property: "row-gap", scale: "spacing" },
  // Position utilities
  "fl-inset": { property: "inset", scale: "spacing", supportsNegative: true },
  "fl-inset-x": {
    property: ["left", "right"],
    scale: "spacing",
    supportsNegative: true
  },
  "fl-inset-y": {
    property: ["top", "bottom"],
    scale: "spacing",
    supportsNegative: true
  },
  "fl-top": { property: "top", scale: "spacing", supportsNegative: true },
  "fl-right": { property: "right", scale: "spacing", supportsNegative: true },
  "fl-bottom": { property: "bottom", scale: "spacing", supportsNegative: true },
  "fl-left": { property: "left", scale: "spacing", supportsNegative: true },
  "fl-start": {
    property: "inset-inline-start",
    scale: "spacing",
    supportsNegative: true
  },
  "fl-end": {
    property: "inset-inline-end",
    scale: "spacing",
    supportsNegative: true
  },
  // Border utilities
  "fl-rounded": { property: "border-radius", scale: "borderRadius" },
  "fl-rounded-t": {
    property: ["border-top-left-radius", "border-top-right-radius"],
    scale: "borderRadius"
  },
  "fl-rounded-r": {
    property: ["border-top-right-radius", "border-bottom-right-radius"],
    scale: "borderRadius"
  },
  "fl-rounded-b": {
    property: ["border-bottom-left-radius", "border-bottom-right-radius"],
    scale: "borderRadius"
  },
  "fl-rounded-l": {
    property: ["border-top-left-radius", "border-bottom-left-radius"],
    scale: "borderRadius"
  },
  "fl-rounded-tl": {
    property: "border-top-left-radius",
    scale: "borderRadius"
  },
  "fl-rounded-tr": {
    property: "border-top-right-radius",
    scale: "borderRadius"
  },
  "fl-rounded-br": {
    property: "border-bottom-right-radius",
    scale: "borderRadius"
  },
  "fl-rounded-bl": {
    property: "border-bottom-left-radius",
    scale: "borderRadius"
  },
  "fl-border": { property: "border-width", scale: "borderWidth" },
  "fl-border-t": { property: "border-top-width", scale: "borderWidth" },
  "fl-border-r": { property: "border-right-width", scale: "borderWidth" },
  "fl-border-b": { property: "border-bottom-width", scale: "borderWidth" },
  "fl-border-l": { property: "border-left-width", scale: "borderWidth" },
  // Scroll margin
  "fl-scroll-m": { property: "scroll-margin", scale: "spacing" },
  "fl-scroll-mx": {
    property: ["scroll-margin-left", "scroll-margin-right"],
    scale: "spacing"
  },
  "fl-scroll-my": {
    property: ["scroll-margin-top", "scroll-margin-bottom"],
    scale: "spacing"
  },
  "fl-scroll-mt": { property: "scroll-margin-top", scale: "spacing" },
  "fl-scroll-mr": { property: "scroll-margin-right", scale: "spacing" },
  "fl-scroll-mb": { property: "scroll-margin-bottom", scale: "spacing" },
  "fl-scroll-ml": { property: "scroll-margin-left", scale: "spacing" },
  // Scroll padding
  "fl-scroll-p": { property: "scroll-padding", scale: "spacing" },
  "fl-scroll-px": {
    property: ["scroll-padding-left", "scroll-padding-right"],
    scale: "spacing"
  },
  "fl-scroll-py": {
    property: ["scroll-padding-top", "scroll-padding-bottom"],
    scale: "spacing"
  },
  "fl-scroll-pt": { property: "scroll-padding-top", scale: "spacing" },
  "fl-scroll-pr": { property: "scroll-padding-right", scale: "spacing" },
  "fl-scroll-pb": { property: "scroll-padding-bottom", scale: "spacing" },
  "fl-scroll-pl": { property: "scroll-padding-left", scale: "spacing" },
  // Space between (special handling needed)
  "fl-space-x": { property: "--tw-space-x-reverse", scale: "spacing" },
  "fl-space-y": { property: "--tw-space-y-reverse", scale: "spacing" },
  // Translate (requires transform)
  "fl-translate-x": {
    property: "--tw-translate-x",
    scale: "spacing",
    supportsNegative: true
  },
  "fl-translate-y": {
    property: "--tw-translate-y",
    scale: "spacing",
    supportsNegative: true
  },
  // Basis
  "fl-basis": { property: "flex-basis", scale: "spacing" }
};
function getThemePath(scale) {
  switch (scale) {
    case "fontSize":
      return "fontSize";
    case "lineHeight":
      return "lineHeight";
    case "letterSpacing":
      return "letterSpacing";
    case "borderRadius":
      return "borderRadius";
    case "borderWidth":
      return "borderWidth";
    case "spacing":
    default:
      return "spacing";
  }
}
var defaultSpacing = {
  "0": "0px",
  px: "1px",
  "0.5": "0.125rem",
  "1": "0.25rem",
  "1.5": "0.375rem",
  "2": "0.5rem",
  "2.5": "0.625rem",
  "3": "0.75rem",
  "3.5": "0.875rem",
  "4": "1rem",
  "5": "1.25rem",
  "6": "1.5rem",
  "7": "1.75rem",
  "8": "2rem",
  "9": "2.25rem",
  "10": "2.5rem",
  "11": "2.75rem",
  "12": "3rem",
  "14": "3.5rem",
  "16": "4rem",
  "20": "5rem",
  "24": "6rem",
  "28": "7rem",
  "32": "8rem",
  "36": "9rem",
  "40": "10rem",
  "44": "11rem",
  "48": "12rem",
  "52": "13rem",
  "56": "14rem",
  "60": "15rem",
  "64": "16rem",
  "72": "18rem",
  "80": "20rem",
  "96": "24rem"
};
var defaultFontSize = {
  xs: "0.75rem",
  sm: "0.875rem",
  base: "1rem",
  lg: "1.125rem",
  xl: "1.25rem",
  "2xl": "1.5rem",
  "3xl": "1.875rem",
  "4xl": "2.25rem",
  "5xl": "3rem",
  "6xl": "3.75rem",
  "7xl": "4.5rem",
  "8xl": "6rem",
  "9xl": "8rem"
};
var defaultLineHeight = {
  none: "1",
  tight: "1.25",
  snug: "1.375",
  normal: "1.5",
  relaxed: "1.625",
  loose: "2",
  "3": "0.75rem",
  "4": "1rem",
  "5": "1.25rem",
  "6": "1.5rem",
  "7": "1.75rem",
  "8": "2rem",
  "9": "2.25rem",
  "10": "2.5rem"
};
var defaultLetterSpacing = {
  tighter: "-0.05em",
  tight: "-0.025em",
  normal: "0em",
  wide: "0.025em",
  wider: "0.05em",
  widest: "0.1em"
};
var defaultBorderRadius = {
  none: "0px",
  sm: "0.125rem",
  DEFAULT: "0.25rem",
  md: "0.375rem",
  lg: "0.5rem",
  xl: "0.75rem",
  "2xl": "1rem",
  "3xl": "1.5rem",
  full: "9999px"
};
var defaultBorderWidth = {
  DEFAULT: "1px",
  "0": "0px",
  "2": "2px",
  "4": "4px",
  "8": "8px"
};
function getDefaultScale(scale) {
  switch (scale) {
    case "fontSize":
      return defaultFontSize;
    case "lineHeight":
      return defaultLineHeight;
    case "letterSpacing":
      return defaultLetterSpacing;
    case "borderRadius":
      return defaultBorderRadius;
    case "borderWidth":
      return defaultBorderWidth;
    case "spacing":
    default:
      return defaultSpacing;
  }
}

// src/validation.ts
var SUPPORTED_UNITS = ["rem", "px", "em"];
function isSupportedUnit(unit) {
  return SUPPORTED_UNITS.includes(unit);
}
function validateUnitsMatch(start, end) {
  const startLen = start instanceof Length ? start : Length.parse(start);
  const endLen = end instanceof Length ? end : Length.parse(end);
  if (!startLen) {
    return {
      valid: false,
      error: FluidError.fromCode("invalid-min", String(start))
    };
  }
  if (!endLen) {
    return {
      valid: false,
      error: FluidError.fromCode("invalid-max", String(end))
    };
  }
  if (startLen.number === 0 || endLen.number === 0) {
    return { valid: true };
  }
  if (startLen.unit !== endLen.unit) {
    return {
      valid: false,
      error: FluidError.fromCode(
        "mismatched-units",
        startLen.cssText,
        endLen.cssText
      )
    };
  }
  if (!isSupportedUnit(startLen.unit)) {
    return {
      valid: false,
      error: FluidError.fromCode("unsupported-unit", startLen.unit || "none")
    };
  }
  return { valid: true };
}
function validateBreakpointsCompatible(value, minViewport, maxViewport) {
  const len = value instanceof Length ? value : Length.parse(value);
  if (!len) {
    return {
      valid: false,
      error: FluidError.fromCode("invalid-min", String(value))
    };
  }
  const unit = len.unit;
  if (!unit) {
    return { valid: true };
  }
  if (minViewport >= maxViewport) {
    return {
      valid: false,
      error: FluidError.fromCode("no-change-bp", `${minViewport}px`)
    };
  }
  return { valid: true };
}
function validateValuesAreDifferent(start, end) {
  const startLen = start instanceof Length ? start : Length.parse(start);
  const endLen = end instanceof Length ? end : Length.parse(end);
  if (!startLen || !endLen) {
    return { valid: true };
  }
  if (startLen.number === endLen.number && startLen.unit === endLen.unit) {
    return {
      valid: false,
      error: FluidError.fromCode("no-change", startLen.cssText)
    };
  }
  return { valid: true };
}
function validateFluidPair(minValue, maxValue, options) {
  const unitsResult = validateUnitsMatch(minValue, maxValue);
  if (!unitsResult.valid) return unitsResult;
  const diffResult = validateValuesAreDifferent(minValue, maxValue);
  if (!diffResult.valid) return diffResult;
  const bpResult = validateBreakpointsCompatible(
    minValue,
    options.minViewport,
    options.maxViewport
  );
  if (!bpResult.valid) return bpResult;
  return { valid: true };
}
function parseAndValidateFluidPair(value, themeValues, options) {
  const parts = value.split("/");
  if (parts.length !== 2) {
    return {
      valid: false,
      error: FluidError.fromCode("parse-failed", value)
    };
  }
  const [minKey, maxKey] = parts.map((s) => s.trim());
  if (!minKey || !maxKey) {
    return {
      valid: false,
      error: FluidError.fromCode("parse-failed", value)
    };
  }
  const minResolved = resolveValue(minKey, themeValues);
  const maxResolved = resolveValue(maxKey, themeValues);
  if (!minResolved) {
    return {
      valid: false,
      error: FluidError.fromCode("theme-value-not-found", minKey)
    };
  }
  if (!maxResolved) {
    return {
      valid: false,
      error: FluidError.fromCode("theme-value-not-found", maxKey)
    };
  }
  const validation = validateFluidPair(minResolved, maxResolved, options);
  if (!validation.valid) {
    return validation;
  }
  return {
    min: Length.parse(minResolved),
    max: Length.parse(maxResolved),
    minKey,
    maxKey,
    minResolved,
    maxResolved
  };
}
function resolveValue(key, themeValues) {
  const parsed = Length.parse(key);
  if (parsed?.unit) {
    return key;
  }
  const themeValue = themeValues[key];
  if (themeValue !== void 0) {
    if (typeof themeValue === "string") {
      return themeValue;
    }
    if (Array.isArray(themeValue) && typeof themeValue[0] === "string") {
      return themeValue[0];
    }
    if (typeof themeValue === "object" && themeValue !== null) {
      const obj = themeValue;
      for (const prop of ["fontSize", "value", "size"]) {
        if (typeof obj[prop] === "string") {
          return obj[prop];
        }
      }
    }
  }
  if (/^-?\d+(\.\d+)?$/.test(key)) {
    const num = parseFloat(key);
    return `${num * 0.25}rem`;
  }
  return null;
}
function createDebugComment(minValue, maxValue, minViewport, maxViewport, isContainer = false) {
  const type = isContainer ? "container" : "viewport";
  return `/* fluid from ${minValue} at ${minViewport}px to ${maxValue} at ${maxViewport}px (${type}) */`;
}
function parseArbitraryValue(value) {
  const match = value.match(/^\[(.*?)\]$/);
  return match ? match[1] : null;
}
function isArbitraryValue(value) {
  return /^\[.*\]$/.test(value);
}

// src/index.ts
var defaultOptions = {
  minViewport: 375,
  maxViewport: 1440,
  useRem: true,
  rootFontSize: 16,
  checkAccessibility: true,
  prefix: "",
  separator: ":",
  useContainerQuery: false,
  debug: false,
  validateUnits: true
};
function normalizeOptions(options) {
  const normalized = { ...options };
  if (options.minviewport !== void 0 && options.minViewport === void 0) {
    normalized.minViewport = options.minviewport;
  }
  if (options.maxviewport !== void 0 && options.maxViewport === void 0) {
    normalized.maxViewport = options.maxviewport;
  }
  if (options.userem !== void 0 && options.useRem === void 0) {
    normalized.useRem = options.userem;
  }
  if (options.rootfontsize !== void 0 && options.rootFontSize === void 0) {
    normalized.rootFontSize = options.rootfontsize;
  }
  if (options.checkaccessibility !== void 0 && options.checkAccessibility === void 0) {
    normalized.checkAccessibility = options.checkaccessibility;
  }
  if (options.usecontainerquery !== void 0 && options.useContainerQuery === void 0) {
    normalized.useContainerQuery = options.usecontainerquery;
  }
  if (options.validateunits !== void 0 && options.validateUnits === void 0) {
    normalized.validateUnits = options.validateunits;
  }
  return normalized;
}
function resolveOptions(options = {}) {
  const normalized = normalizeOptions(options);
  return {
    ...defaultOptions,
    ...normalized
  };
}
var SKIP_KEYS = /* @__PURE__ */ new Set(["DEFAULT", "none", "full"]);
function extractUnit(value) {
  if (typeof value === "string") {
    const parsed = Length.parse(value.trim());
    if (parsed?.unit) return parsed.unit;
    return null;
  }
  if (Array.isArray(value) && value.length > 0) {
    return extractUnit(value[0]);
  }
  if (value !== null && typeof value === "object" && !Array.isArray(value)) {
    const obj = value;
    for (const prop of ["fontSize", "value", "size"]) {
      if (typeof obj[prop] === "string") return extractUnit(obj[prop]);
    }
  }
  return null;
}
function generateFluidValues(scaleValues, debug = false) {
  const values = {};
  const keys = Object.keys(scaleValues);
  const unitByKey = /* @__PURE__ */ new Map();
  const unparsableKeys = [];
  for (const key of keys) {
    if (SKIP_KEYS.has(key)) continue;
    const unit = extractUnit(scaleValues[key]);
    if (unit) {
      unitByKey.set(key, unit);
    } else {
      unparsableKeys.push(key);
    }
  }
  const keysByUnit = /* @__PURE__ */ new Map();
  for (const [key, unit] of unitByKey) {
    let group = keysByUnit.get(unit);
    if (!group) {
      group = [];
      keysByUnit.set(unit, group);
    }
    group.push(key);
  }
  if (debug) {
    if (keysByUnit.size > 1) {
      const unitSummary = [...keysByUnit.entries()].map(([unit, group]) => `${unit} (${group.length} values)`).join(", ");
      console.warn(
        `[fluid-tailwindcss] Theme scale contains mixed units: ${unitSummary}. Cross-unit pairs have been excluded.`
      );
    }
    if (unparsableKeys.length > 0) {
      console.warn(
        `[fluid-tailwindcss] Could not determine unit for theme keys: ${unparsableKeys.join(", ")}. These keys are excluded from fluid value generation.`
      );
    }
  }
  for (const key of unitByKey.keys()) {
    values[key] = key;
  }
  return values;
}
function createFluidDeclaration(properties, clampValue) {
  const props = Array.isArray(properties) ? properties : [properties];
  const result = {};
  for (const prop of props) {
    result[prop] = clampValue;
  }
  return result;
}
var fluidPlugin = plugin__default.default.withOptions(
  (options = {}) => {
    const resolvedOptions = resolveOptions(options);
    return ({ matchUtilities, theme }) => {
      for (const [utilityName, utilityDef] of Object.entries(fluidUtilities)) {
        const themePath = getThemePath(utilityDef.scale);
        const themeValues = theme(themePath) ?? getDefaultScale(utilityDef.scale);
        const fluidValues = generateFluidValues(
          themeValues,
          resolvedOptions.debug
        );
        if (utilityName === "fl-space-x" || utilityName === "fl-space-y") {
          registerSpaceUtility(
            utilityName,
            utilityDef,
            themeValues,
            fluidValues,
            resolvedOptions,
            matchUtilities
          );
          continue;
        }
        if (utilityName === "fl-translate-x" || utilityName === "fl-translate-y") {
          registerTranslateUtility(
            utilityName,
            utilityDef,
            themeValues,
            fluidValues,
            resolvedOptions,
            matchUtilities
          );
          continue;
        }
        const fullUtilityName = resolvedOptions.prefix ? `${resolvedOptions.prefix}${utilityName}` : utilityName;
        const createUtilityHandler = (negate) => {
          return (value, extra) => {
            const effectiveValue = extra.modifier != null ? `${value}/${extra.modifier}` : value;
            const arbitraryValue = parseArbitraryValue(effectiveValue);
            if (arbitraryValue) {
              const arbitraryParsed = parseFluidString(arbitraryValue);
              if (!arbitraryParsed) return {};
              if (resolvedOptions.validateUnits) {
                const validation = validateFluidUnits(
                  arbitraryParsed.min,
                  arbitraryParsed.max
                );
                if (!validation.valid) {
                  console.warn(
                    `[fluid-tailwindcss] ${validation.error?.message}`
                  );
                  return {};
                }
              }
              const { result } = calculateClampAdvanced(
                arbitraryParsed.min,
                arbitraryParsed.max,
                resolvedOptions,
                { negate }
              );
              return createFluidDeclaration(utilityDef.property, result);
            }
            const parsed = parseFluidString(effectiveValue);
            if (!parsed) return {};
            const minResolved = resolveThemeValue(parsed.min, themeValues);
            const maxResolved = resolveThemeValue(parsed.max, themeValues);
            if (!minResolved || !maxResolved) return {};
            if (resolvedOptions.validateUnits) {
              const validation = validateFluidUnits(minResolved, maxResolved);
              if (!validation.valid) {
                if (resolvedOptions.debug) {
                  console.warn(
                    `[fluid-tailwindcss] ${validation.error?.message}`
                  );
                }
                return {};
              }
            }
            if (utilityName === "fl-text") {
              const { warning } = checkAccessibility(
                minResolved,
                resolvedOptions,
                "text"
              );
              if (warning) {
                console.warn(`[fluid-tailwindcss] ${warning}`);
              }
            }
            const { result: clampValue } = calculateClampAdvanced(
              minResolved,
              maxResolved,
              resolvedOptions,
              { negate }
            );
            return createFluidDeclaration(utilityDef.property, clampValue);
          };
        };
        matchUtilities(
          {
            [fullUtilityName]: createUtilityHandler(false)
          },
          {
            values: fluidValues,
            modifiers: "any"
          }
        );
        if (utilityDef.supportsNegative) {
          const negativeUtilityName = `neg-${fullUtilityName}`;
          matchUtilities(
            {
              [negativeUtilityName]: createUtilityHandler(true)
            },
            {
              values: fluidValues,
              modifiers: "any"
            }
          );
        }
      }
    };
  },
  // Plugin configuration (for @plugin options)
  () => ({
    // No additional Tailwind config needed
  })
);
function registerSpaceUtility(utilityName, _utilityDef, themeValues, fluidValues, resolvedOptions, matchUtilities) {
  const isX = utilityName === "fl-space-x";
  const marginProp = isX ? "margin-left" : "margin-top";
  const fullUtilityName = resolvedOptions.prefix ? `${resolvedOptions.prefix}${utilityName}` : utilityName;
  const createSpaceHandler = (negate) => {
    return (value, extra) => {
      const effectiveValue = extra.modifier != null ? `${value}/${extra.modifier}` : value;
      const arbitraryValue = parseArbitraryValue(effectiveValue);
      if (arbitraryValue) {
        const arbitraryParsed = parseFluidString(arbitraryValue);
        if (!arbitraryParsed) return {};
        if (resolvedOptions.validateUnits) {
          const validation = validateFluidUnits(
            arbitraryParsed.min,
            arbitraryParsed.max
          );
          if (!validation.valid) return {};
        }
        const { result } = calculateClampAdvanced(
          arbitraryParsed.min,
          arbitraryParsed.max,
          resolvedOptions,
          { negate }
        );
        return {
          "& > :not([hidden]) ~ :not([hidden])": {
            [marginProp]: result
          }
        };
      }
      const parsed = parseFluidString(effectiveValue);
      if (!parsed) return {};
      const minResolved = resolveThemeValue(parsed.min, themeValues);
      const maxResolved = resolveThemeValue(parsed.max, themeValues);
      if (!minResolved || !maxResolved) return {};
      if (resolvedOptions.validateUnits) {
        const validation = validateFluidUnits(minResolved, maxResolved);
        if (!validation.valid) return {};
      }
      const { result: clampValue } = calculateClampAdvanced(
        minResolved,
        maxResolved,
        resolvedOptions,
        { negate }
      );
      return {
        "& > :not([hidden]) ~ :not([hidden])": {
          [marginProp]: clampValue
        }
      };
    };
  };
  matchUtilities(
    {
      [fullUtilityName]: createSpaceHandler(false)
    },
    {
      values: fluidValues,
      modifiers: "any"
    }
  );
  const negativeUtilityName = `neg-${fullUtilityName}`;
  matchUtilities(
    {
      [negativeUtilityName]: createSpaceHandler(true)
    },
    {
      values: fluidValues,
      modifiers: "any"
    }
  );
}
function registerTranslateUtility(utilityName, utilityDef, themeValues, fluidValues, resolvedOptions, matchUtilities) {
  const fullUtilityName = resolvedOptions.prefix ? `${resolvedOptions.prefix}${utilityName}` : utilityName;
  const createTranslateHandler = (negate) => {
    return (value, extra) => {
      const effectiveValue = extra.modifier != null ? `${value}/${extra.modifier}` : value;
      const arbitraryValue = parseArbitraryValue(effectiveValue);
      if (arbitraryValue) {
        const arbitraryParsed = parseFluidString(arbitraryValue);
        if (!arbitraryParsed) return {};
        if (resolvedOptions.validateUnits) {
          const validation = validateFluidUnits(
            arbitraryParsed.min,
            arbitraryParsed.max
          );
          if (!validation.valid) return {};
        }
        const { result } = calculateClampAdvanced(
          arbitraryParsed.min,
          arbitraryParsed.max,
          resolvedOptions,
          { negate }
        );
        return {
          [utilityDef.property]: result,
          transform: `translate(var(--tw-translate-x), var(--tw-translate-y)) rotate(var(--tw-rotate)) skewX(var(--tw-skew-x)) skewY(var(--tw-skew-y)) scaleX(var(--tw-scale-x)) scaleY(var(--tw-scale-y))`
        };
      }
      const parsed = parseFluidString(effectiveValue);
      if (!parsed) return {};
      const minResolved = resolveThemeValue(parsed.min, themeValues);
      const maxResolved = resolveThemeValue(parsed.max, themeValues);
      if (!minResolved || !maxResolved) return {};
      if (resolvedOptions.validateUnits) {
        const validation = validateFluidUnits(minResolved, maxResolved);
        if (!validation.valid) return {};
      }
      const { result: clampValue } = calculateClampAdvanced(
        minResolved,
        maxResolved,
        resolvedOptions,
        { negate }
      );
      return {
        [utilityDef.property]: clampValue,
        transform: `translate(var(--tw-translate-x), var(--tw-translate-y)) rotate(var(--tw-rotate)) skewX(var(--tw-skew-x)) skewY(var(--tw-skew-y)) scaleX(var(--tw-scale-x)) scaleY(var(--tw-scale-y))`
      };
    };
  };
  matchUtilities(
    {
      [fullUtilityName]: createTranslateHandler(false)
    },
    {
      values: fluidValues,
      modifiers: "any"
    }
  );
  if (utilityDef.supportsNegative) {
    const negativeUtilityName = `neg-${fullUtilityName}`;
    matchUtilities(
      {
        [negativeUtilityName]: createTranslateHandler(true)
      },
      {
        values: fluidValues,
        modifiers: "any"
      }
    );
  }
}
var src_default = fluidPlugin;

exports.FluidError = FluidError;
exports.Length = Length;
exports.SUPPORTED_UNITS = SUPPORTED_UNITS;
exports.calculateClamp = calculateClamp;
exports.calculateClampAdvanced = calculateClampAdvanced;
exports.calculateNegativeClamp = calculateNegativeClamp;
exports.checkAccessibility = checkAccessibility;
exports.checkSC144 = checkSC144;
exports.clampNumber = clampNumber;
exports.createContainerClamp = createContainerClamp;
exports.createDebugComment = createDebugComment;
exports.createNegatedClamp = createNegatedClamp;
exports.default = src_default;
exports.err = err;
exports.errorCodes = errorCodes;
exports.extractUnit = extractUnit;
exports.fluidPlugin = fluidPlugin;
exports.fluidUtilities = fluidUtilities;
exports.getDefaultScale = getDefaultScale;
exports.getPrecision = getPrecision;
exports.isArbitraryValue = isArbitraryValue;
exports.isSupportedUnit = isSupportedUnit;
exports.ok = ok;
exports.parseAndValidateFluidPair = parseAndValidateFluidPair;
exports.parseArbitraryValue = parseArbitraryValue;
exports.parseFluidString = parseFluidString;
exports.resolveThemeValue = resolveThemeValue;
exports.throwError = throwError;
exports.toPrecision = toPrecision;
exports.validateBreakpointsCompatible = validateBreakpointsCompatible;
exports.validateFluidPair = validateFluidPair;
exports.validateFluidUnits = validateFluidUnits;
exports.validateUnitsMatch = validateUnitsMatch;
exports.validateValuesAreDifferent = validateValuesAreDifferent;
//# sourceMappingURL=index.cjs.map
//# sourceMappingURL=index.cjs.map